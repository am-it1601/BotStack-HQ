# Agent.md — Backend

This file is the generic-agent entry point. **Read [`Claude.md`](./Claude.md) in this directory** — it is the authoritative router for working in the backend service (context-loading order, non-negotiables, report/backlog rules).

All agents (Claude, Cursor, Copilot, etc.) working in `apps/backend` follow `Claude.md`.
