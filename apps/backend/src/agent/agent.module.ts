import { Module } from '@nestjs/common';

/**
 * Agent domain — AI agent conversation orchestration over WhatsApp, driving the
 * client-facing flows defined in the Agent Flow Specs.
 *
 * Scaffold only. Controllers and services are added in their dedicated tasks.
 */
@Module({})
export class AgentModule {}
