import { Module } from '@nestjs/common';

/**
 * Notification domain (ADD §04) — WhatsApp message dispatch and confirmation
 * delivery to clients.
 *
 * Scaffold only. Controllers and services are added in their dedicated tasks.
 */
@Module({})
export class NotificationModule {}
