import { Module } from '@nestjs/common';

/**
 * Filing domain (ADD §04) — filing types, calendar, deadlines, and the filing
 * dependency chain.
 *
 * Scaffold only. Controllers and services are added in their dedicated tasks.
 */
@Module({})
export class FilingModule {}
