import { Module } from '@nestjs/common';

/**
 * Document domain (ADD §04) — document upload, S3 integration, and the
 * extraction trigger into the Document AI service.
 *
 * Scaffold only. Controllers and services are added in their dedicated tasks.
 */
@Module({})
export class DocumentModule {}
