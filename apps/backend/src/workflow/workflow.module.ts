import { Module } from '@nestjs/common';

/**
 * Workflow domain (ADD §04) — input-collection workflows and reminder sequences.
 *
 * Scaffold only. Controllers and services are added in their dedicated tasks.
 */
@Module({})
export class WorkflowModule {}
