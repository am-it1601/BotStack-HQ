# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
out/
cdk.out/
*.tsbuildinfo

# Turborepo
.turbo/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Local Python runtime auto-installed by the Windows `python` app-execution shim
# during `turbo build` (document-ai). Not part of the repo — never commit it.
apps/document-ai/Python/

# Environment
.env
.env.*
!.env.example

# Local secrets — never commit (holds live API keys / tokens)
key.yml

# Logs
*.log
npm-debug.log*

# Coverage
coverage/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
.claude/
